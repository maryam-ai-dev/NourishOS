{
  "buildCommand": "echo 'Serving prebuilt frontend/build/web — rebuild locally with flutter build web --release and commit before pushing.'",
  "outputDirectory": "frontend/build/web",
  "installCommand": "true",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
